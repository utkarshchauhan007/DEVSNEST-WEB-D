sql
CREATE USER devsnest WITH PASSWORD 'password';
CREATE DATABASE devs;
GRANT ALL PRIVILAGES ON DATABASE devs to devsnest;

DROP DATABASE IF EXISTS devs;

//FLODER
CREATE SCHEMA mySchema;
CREATE TABLE mySchema COMPANY
(
    ID INT NOT NULL;
    NAME VARCHAR[20] NOT NULL;
    AGE INT NOT NULL;
    ADDRESS CHAR[25];
    SALARY DECIMAL(18,2);
    PRIMARY KEY(ID); 
);

DROP SCHEMA mySchema CASCADE;
INSERT INTO COMPANY
( ID, NAME, AGE, ADDRESS, SALARY, JOIN_DATE) VALUES (1, 'PAUL', 32, 'CALIFORNIA', 2000.0, '2001-07-13');

DROP DATABASE IF EXISTS COMPANY;
DROP TABLE IF EXISTS COMPANY;